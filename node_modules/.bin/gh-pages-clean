#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../gh-pages/bin/gh-pages-clean.js" "$@"
  ret=$?
else 
  node  "$basedir/../gh-pages/bin/gh-pages-clean.js" "$@"
  ret=$?
fi
exit $ret
