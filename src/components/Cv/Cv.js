function Cv() {
  console.log('CV page')
  // remove /cv from url
  // const domain = window.location.href.replace(/\/cv$/, '')
  // console.log({ domain })
  // window.location.href = `${domain}/nmpereira_cv.pdf`

  return <h1>CV</h1>
}

export default Cv
